# Expo / React Native + Metro do not play well with pnpm's default isolated,
# symlinked node_modules. A hoisted (npm-like) layout is the Expo-recommended
# setup for pnpm monorepos and makes Metro resolution reliable.
node-linker=hoisted
